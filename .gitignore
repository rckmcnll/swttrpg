# OggDude DataSet — copyrighted third-party app/data, ~134 MB (exceeds GitHub limits). Never commit.
SWCharGen.zip
SWCharGen/

# Local full-description catalog override (may contain copyrighted rulebook text). Keep local only.
fad-catalog.local.js
fad-catalog.local.zip

# Local backups / scratch artifacts
*_old.html
*_sheet_backup.json

# Single-file bundle from tools/build-bundle.mjs — embeds the local catalog
# (may contain copyrighted rulebook text). Personal use only; never commit.
*.bundled.html

# Local tooling / agent memory
.memsearch/
